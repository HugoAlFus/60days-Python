### 🧠 Día 51: Clasificador de Texto (NLP + Sklearn)

Crea un modelo de **Machine Learning** que clasifica texto (como spam/no spam, opiniones positivas/negativas).

**Módulos necesarios**:

- `scikit-learn`
    
- `nltk` o `textblob`
    
- `pandas`