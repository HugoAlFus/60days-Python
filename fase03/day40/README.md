# 📘 Día 40: Chat en Tiempo Real (SocketIO)

Una app de chat que permite enviar y recibir mensajes en tiempo real entre múltiples usuarios usando WebSockets.

**Módulos necesarios**:

- `flask`

- `flask_socketio`

- `eventlet` o `gevent`